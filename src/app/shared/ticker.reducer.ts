import {Action} from '@ngrx/store';

const initialState = {};
export function tickerReducer(state: any = initialState, action: Action) {
  switch (action.type) {
    default:
      return state;
  }
}
